# Pre-commit config

repos:
  - repo: https://github.com/google/yapf
    rev: v0.40.1 # Use the latest stable version
    hooks:
      - id: yapf
        name: yapf
        args: ['--style', 'google'] # This argument specifies the Google style
